#include <iostream>

struct Employee;
int sumTo(const int number);
void printEmployeeName(const Employee &employee);
void minmax(const int fistNumber, const int secondNumber, int &minOut,int &maxOut);
int getIndexOfLargestValue(const int *array, const int arraySize);
const int& getElement(const int *array, const int arraySize);

int main()
{
    return 0;
}
