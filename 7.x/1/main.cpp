#include <iostream>

double max(double firstNumber, double secondNumber);
void swap(int &firstNumber, int &secondNumber);
int& getLargestElement(int *array, int arrayLengt);

int main()
{
    return 0;
}
